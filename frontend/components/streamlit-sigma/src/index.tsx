import React from "react";
import ReactDOM from "react-dom/client";
import StreamlitSigma from "./streamlit_sigma";


ReactDOM.createRoot(document.getElementById("root")!).render(
<React.StrictMode>
    <StreamlitSigma />
</React.StrictMode>
);
